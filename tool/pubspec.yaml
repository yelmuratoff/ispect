name: ispect_tool
description: Release, version, and documentation tooling for the ISpect monorepo.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  pub_semver: ^2.2.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.31.2
