/// WebSocket message direction marker for outgoing (sent) messages.
const String wsTypeRequest = 'REQUEST';

/// WebSocket message direction marker for incoming (received) messages.
const String wsTypeResponse = 'RESPONSE';
