name: ispectify_http
description: An additional package for http (logging and handling).
version: 4.2.1-dev13
homepage: https://github.com/yelmuratoff/ispect.git
issue_tracker: https://github.com/yelmuratoff/ispect/issues

environment:
  sdk: ">=3.6.0 <4.0.0"

dependency_overrides:
  ispectify:
    path: ../ispectify

dependencies:
  http_interceptor: ^2.0.0
  ispectify: ^4.2.1-dev13

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
