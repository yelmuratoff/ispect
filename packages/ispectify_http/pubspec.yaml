name: ispectify_http
description: Lightweight and customizable http client logger on iSpectify base
version: 0.1.0-dev.21
homepage: https://github.com/yelmuratoff/ispect.git
issue_tracker: https://github.com/yelmuratoff/ispect/issues
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
resolution: workspace

dependencies:
  http_interceptor: ^2.0.0
  ispectify:
    path: ../ispectify

dev_dependencies:
  lints: ^5.0.0
  test: ^1.21.0
